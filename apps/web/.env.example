# Copy to `.env` (or set in Cloudflare Pages build settings).
#
# Dev: leave blank — the Vite proxy forwards /api to the local Worker.
# Prod: set to your deployed Worker URL, e.g. https://r2-media-server-api.<you>.workers.dev
VITE_API_URL=
